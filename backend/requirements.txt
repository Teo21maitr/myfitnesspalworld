# Dépendances de production — versions épinglées (spec 10 §1).
# Toute mise à jour doit être délibérée et testée.

# Framework
Django==5.2.9
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1

# Base de données
psycopg[binary]==3.3.4

# Configuration
django-environ==0.14.0

# Parsing XML durci, utilisé par l'import Ciqual
defusedxml==0.7.1

# Client HTTP des sources externes (Open Food Facts)
requests==2.34.2

# Fournisseur d'IA (spec 07 §2). Isolé derrière `ai.providers.AnthropicProvider`
# et importé paresseusement : le socle démarre sans lui.
anthropic==1.2.0

# Rapports PDF (spec 01 §22). Polices Type 1 intégrées : les accents sortent
# sans embarquer de fichier de police, et rien à installer sur l'hôte.
reportlab==5.0.1

# Stockage objet S3-compatible : photos de progression (spec 05 §10).
boto3==1.43.83

# Traitement des images envoyées : redimensionnement et retrait de l'EXIF.
Pillow==12.3.0

# Tâches asynchrones
celery[redis]==5.6.3
# redis-py est contraint par kombu (celery) : redis<6.5
redis==6.4.0

# HTTP / sécurité
django-cors-headers==4.9.0

# Envoi d'emails par API HTTP. Le SMTP est fermé en sortie sur la plateforme :
# un fournisseur qui ne parle que SMTP y est inutilisable (spec 09).
django-anymail==15.1

# Service des fichiers statiques (admin) en production
whitenoise==6.12.0

# Serveur WSGI de production
gunicorn==26.1.0

# L'Anses publie la table Ciqual en 7z, format que la bibliothèque standard ne
# lit pas. Nécessaire en production : l'import tourne dans le conteneur.
py7zr==1.1.3
